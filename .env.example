# Copia a .env (gitignored) y completá. Render local de critters → S3 (s3.dotrino.com).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
# Opcionales (tienen default en el código):
# BUCKET=s3.dotrino.com
# PREFIX=critters/
